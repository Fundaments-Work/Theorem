# Theorem Stabilization Roadmap: Native-Grade PDF Parity, Rust Core Architecture & Core Resilience (v1.5.6 → v1.6.0)

**Date**: 2026-09-17  
**Status**: Active Core Stabilization Plan (Standalone Core Document — Plugin Architecture Separated)  
**Scope**: v1.5.6 (PDF Native Parity & Viewport Resilience) $\rightarrow$ v1.5.7–v1.5.9 (Mobile, Sync & Edge-Case Polish) $\rightarrow$ v1.6.0 (Universal PKM Templates & Stable Release)  
**Companion Documents**:
- Plugins: [`docs/plans/2026-08-30-plugin-extensibility-architecture.md`](./2026-08-30-plugin-extensibility-architecture.md) (Dedicated WASM & Extensibility Architecture for v2.0.0+)
- Native Readability & Knap: [`docs/plans/2026-09-17-native-rust-readability-and-knap-templating.md`](./2026-09-17-native-rust-readability-and-knap-templating.md) (Rust-First Article Extraction & Knap AST Note Templating)
**Authors**: Theorem Core Team  

---

## 1. Executive Strategy & Guiding Decisions

Theorem's roadmap from **v1.5.5 to v1.6.0** is focused on **rock-solid stability, native performance parity, and universal knowledge extensibility**:

### 1.1 Fast Reading vs. Bionic Reading Clarification
- **Fast Reading Mode (RSVP / Speed Read - Built-in)**: Theorem already includes an RSVP (Rapid Serial Visual Presentation) mode in `src/features/reader/components/SpeedReaderModal.tsx`. RSVP flashes words sequentially at a fixed visual focal point at 300–800+ WPM, eliminating saccadic eye movement and sub-vocalization fatigue. This feature is preserved and accessible via reader controls.
- **Bionic Reading (Dropped from Roadmap)**: Bionic reading alters page typography by artificially bolding the initial syllables of every word (e.g., **The**orem **del**ivers **fas**ter **read**ing). Because Theorem readers prioritize clean, unadorned typography and Theorem already provides native RSVP speed reading, artificial font bolding adds visual noise and DOM complexity without verifiable comprehension benefits. It is dropped from the roadmap.

### 1.2 Spaced Repetition: Theorem Lemma SRS vs. Anki Exporter
- **Lemma SRS Integration**: Theorem's vocabulary workspace already synchronizes terms and definitions with **Lemma**, our companion spaced-repetition tool.
- **No Redundant Anki Exporter**: Rather than maintaining an isolated Anki `.apkg` generator that fragments development focus, vocabulary and highlights are exported as clean, standard Markdown that any flashcard tool or the Lemma companion can consume directly.

### 1.3 Universal PKM / Markdown Systems & Knap AST Templating
- Theorem is not an "Obsidian-only" reader. It is the premier reading hub for **all Markdown-based Personal Knowledge Management (PKM) systems**, including **Obsidian, Logseq, Foam, SilverBullet, Anytype, Dendron, Joplin, and plain Neovim/Emacs directories**.
- All note and highlight exports use standard Markdown, standard YAML frontmatter (`---`), and portable callouts (`> [!note]`, `> [!quote]`). In **v1.6.0**, **`knap`** (Obsidian's AST template engine) will provide a secure, sandboxed template compiler allowing users to format notes according to any tool's preferred schema with Markdown-native filters (`wikilink`, `callout`, `date`, `yaml`).
- **Deferred Items**:
  - *Branded Theorem Web Clipper*: While `obsidian-clipper` is MIT-licensed, creating a custom-branded Theorem Web Clipper browser extension is deferred alongside the WebAssembly plugin system until core reading and sync are stabilized.
  - *Web Article & RSS Vault Export*: Exporting clipped web articles and RSS feeds directly into vault note files is deferred until native Rust article extraction and book highlight vault sync are verified in production.

### 1.4 Strict Architectural Separation from Plugins
- In accordance with core engineering principles, **plugin extensibility is strictly separated from the core stabilization roadmap**. Core reading, PDF rendering, SQLite database virtualization, and P2P sync must achieve 100% native stability before any third-party sandbox is opened.
- All WebAssembly plugin ecosystem architecture is maintained in its own dedicated document: [`docs/plans/2026-08-30-plugin-extensibility-architecture.md`](./2026-08-30-plugin-extensibility-architecture.md).

---

## 2. Core Architectural Upgrades: Native-Grade PDF Parity (v1.5.6)

In **v1.5.6**, PDF reading is brought to **true native parity** with platforms like macOS Preview, Okular, and browser PDF readers (Mozilla PDF.js):

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                    NATIVE-GRADE PDF ARCHITECTURAL UPGRADES                      │
├──────────────────────────┬──────────────────────────────────────────────────────┤
│ SUBSYSTEM                │ NATIVE IMPLEMENTATION IN v1.5.6                      │
├──────────────────────────┼──────────────────────────────────────────────────────┤
│ 1. True Full-Document    │ Pre-compute document layout for all N pages via Rust │
│    Virtualization        │ `prefetch_pdf_structure` command. Zero layout shift. │
│                          │ Sized placeholders preserve exact virtual tops.      │
├──────────────────────────┼──────────────────────────────────────────────────────┤
│ 2. Instant Navigation    │ Jumping to Page 450 calculates targetTop = Y[450]    │
│    (TOC / Page Jumps)    │ instantly (0ms) without waiting for sequential batch │
│                          │ page loading. Direct scrollToPage with near targets. │
├──────────────────────────┼──────────────────────────────────────────────────────┤
│ 3. Two-Page Spread Mode  │ Facing Pages presentation mode (`two-page`) for      │
│    (Facing Pages)        │ desktop displays: Facing pairs side-by-side          │
│                          │ (`[1, 2], [3, 4]`), spread-aware zoom/keys, and      │
│                          │ non-clipping `max-content` layout.                   │
├──────────────────────────┼──────────────────────────────────────────────────────┤
│ 4. Trackpad & Wheel Zoom │ Anchored zoom around mouse cursor or pinch center.   │
│    Anchoring             │ Instant CSS hardware transform during active gesture;│
│                          │ debounced high-DPI canvas re-rasterization on end.   │
├──────────────────────────┼──────────────────────────────────────────────────────┤
│ 5. Outline TOC Tracking  │ Continuous active chapter synchronization: computes  │
│                          │ nearest TOC item (`itemPage <= currentPage`) to keep │
│                          │ the active chapter highlighted on continuous scroll. │
├──────────────────────────┼──────────────────────────────────────────────────────┤
│ 6. Search Match Focus    │ Native Rust search (`search_book_content`) jumps to  │
│    Rings & Rings         │ page; text layer renders pulsing `.pdf-search-match` │
│                          │ focus rings and tinted highlights on query words.    │
├──────────────────────────┼──────────────────────────────────────────────────────┤
│ 7. Fullscreen Overlay    │ Static fullscreen `absolute inset-0 z-0 isolate`     │
│    Chrome & Stacking     │ ensures EPUB pagination never reflows; Titlebar and  │
│    Context               │ Navbar float at `fixed z-[140]` over the viewport.   │
└──────────────────────────┴──────────────────────────────────────────────────────┘
```

### 2.1 PDF Engine Technical Details (`src/features/reader/engines/pdfjs-engine.tsx`)

1. **Pre-Fetched Structure & Virtual Tops**:
   - `prefetch_pdf_structure` (Rust Tauri command) parses the PDF trailer and root catalog in <5ms, returning `total_pages`, `default_width_pt`, and `default_height_pt`.
   - `computeVirtualPageTops()` calculates an immutable `Float64Array` containing exact scroll positions for all $N$ pages.
   - Placeholders are registered with an `IntersectionObserver` to trigger concurrent proxy loading (`PAGE_PROXY_LOAD_CONCURRENCY = 3`) as pages approach the viewport.

2. **Two-Page Spread (Facing Pages)**:
   - Chunked via `getSpreads(totalPages)`: Spread $k$ is `[2k - 1, 2k]` (e.g. `[1, 2]`, `[3, 4]...`), allowing facing page reading to start immediately on page 1.
   - Each spread row renders `<div className="pdf-spread-row flex flex-row flex-nowrap items-center gap-2 sm:gap-4 my-1 sm:my-2 mx-auto">` with CSS `width: max-content; min-width: 100%; justify-content: safe center;`.
   - The `.pdf-zoom-container` uses `width: max-content; min-width: 100%; margin: 0 auto;`, eliminating centering overflow data loss so the entire left page is visible from $x = 0$ with full horizontal panning and zoom support.
   - `getFitWidthScale(container, page, isTwoPage)` and `getFitPageScale(container, page, isTwoPage)` divide container width by 2 (minus horizontal spread gap) to automatically fit facing pages on widescreen monitors.
   - Next/previous actions (keyboard ArrowLeft/ArrowRight, PageUp/PageDown, boundary-aware touch swipe, edge tap zones) advance by spread in two-page mode without interrupting horizontal panning.

3. **Active TOC Outline Synchronization**:
   - `activePdfTocHref` in `Reader.tsx` continuously scans the table of contents tree to locate the latest chapter where `chapterPage <= pdfCurrentPage`.
   - Avoids the pitfall where TOC highlights vanish as soon as the user scrolls past the first page of a chapter.

4. **Search Match Focus Rings**:
   - `PageCanvas` receives `searchQuery`. When text layer rendering finishes, a DOM walker extracts matching text spans and wraps them with `.pdf-search-match`.
   - CSS rules apply `box-shadow: 0 0 0 1.5px var(--color-accent)`, semi-transparent accent background, and subtle pulsing focus ring, respecting `prefers-reduced-motion`.

5. **Fullscreen Overlay Chrome & Stacking Context Isolation**:
   - Viewport containers in `Reader.tsx`, `ArticleViewer.tsx`, and `ReaderViewport.tsx` remain fullscreen `absolute inset-0 z-0 isolate`, completely decoupled from toolbar or navbar visibility.
   - Prevents EPUB column re-pagination, page jumping, or reflow bugs caused by dynamic viewport dimension resizing.
   - `WindowTitlebar` and `ReaderNavbar` operate as floating HUD overlays rendered at `z-[140]`, gliding smoothly over the content via GPU-accelerated `translate-y` transforms without affecting reading layout.

---

## 3. Core Architectural Subsystems & High-Performance Rust Foundations

### 3.1 Cloudflare-Inspired Rust Data Layouts
To eliminate memory fragmentation and heap overhead across Theorem's native core, we adopt Cloudflare's high-performance memory layout principles:

```
┌─────────────────────────────────────────────────────────────────────────────────────────────────┐
│                           CLOUDFLARE RUST DATA LAYOUT PRINCIPLES                                │
├──────────────────────────┬──────────────────────────────────────────────────────────────────────┤
│ TECHNIQUE                │ APPLICATION IN THEOREM                                               │
├──────────────────────────┼──────────────────────────────────────────────────────────────────────┤
│ 1. Box<[T]> & Box<str>   │ Used for all immutable DTOs (RSS articles, OPDS entries, book        │
│    (Cost of Capacity)    │ search results). Drops 8B capacity per field; zero heap slack.       │
│ 2. Contiguous Flattening │ Replace multiple heap vectors with flat buffers indexed by u16       │
│    (Fewer Lists/Pointers)│ offsets. Pack boolean flags into bitflags structs.                   │
│ 3. Context-Inferred Keys │ Child records (e.g. RSS articles, highlights) omit redundant parent  │
│    (Dropping the Owner)  │ IDs (feed_id, book_id) in memory if known by the query context.      │
│ 4. Enum Variant Boxing   │ Box large/rare enum variants (clippy::large_enum_variant) to shrink  │
│    (Enum Sizing)         │ the entire enum to ≤24 bytes.                                        │
│ 5. Scratchpad Buffers    │ Thread-local reusable serialization scratchpads with exact memcpy    │
│    (Wire Format Packing) │ into Box<[u8]>. Zero-copy IPC transfer.                              │
└──────────────────────────┴──────────────────────────────────────────────────────────────────────┘
```

### 3.2 Modern Relational RSS Subsystem
- **Native Streaming Parser (`src-tauri/src/rss_parser.rs`)**: Zero-copy SAX parsing over byte streams using `quick-xml`. Parses feeds in 2–5ms (vs. 150–400ms in JavaScript), shedding ~175KB of minified JS from the frontend bundle.
- **Relational Content Decoupling (`src-tauri/src/database.rs`)**:
  - `rss_feeds` & `rss_articles`: Store lightweight metadata (~200 bytes per article).
  - `rss_article_content`: Stores heavy HTML content, fetched strictly on-demand when an article is opened.
  - Eliminates the 25MB monolithic persist loop from `rssStore.ts`.
- **Native Readability (`src-tauri/src/article_extractor.rs`)**: Rust-native article extraction from raw HTML.

### 3.3 Hybrid Two-Tier Search Engine (`SQLite FTS5` + `nucleo`)
```
┌─────────────────────────────────────────────────────────────────────────────────────────────────┐
│                     HYBRID TWO-TIER SEARCH ENGINE: FTS5 + NUCLEO                                │
├─────────────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                                 │
│   User Types Search Query: "dune messiah"                                                       │
│                            │                                                                    │
│                            ▼                                                                    │
│   [ TIER 1: SQLite FTS5 (Disk / OS Page Cache) ]                                                │
│   • Runs: `SELECT id, title, author, description, rank                                          │
│            FROM books_fts WHERE books_fts MATCH 'dune*' LIMIT 200`                              │
│   • Scans 50,000+ books in ~1.2ms without loading records into memory.                          │
│   • Fast coarse candidate retrieval (prunes 50,000 items down to top ~200).                    │
│                            │                                                                    │
│                            ▼ Candidate records (200 items, ~40 KB in Rust)                      │
│                                                                                                 │
│   [ TIER 2: nucleo-matcher (SIMD In-Memory Scoring & Highlighting) ]                            │
│   • Runs Helix's SIMD-accelerated Smith-Waterman matcher over candidate records.                │
│   • Applies fine-grained fuzzy scoring: word boundaries, camelCase, typos, transpositions.      │
│   • Extracts exact matched character indices: `Vec<u32>` for UI bolding/underlining.            │
│   • Sorts candidates and takes top N (e.g. 50) in ~0.1ms.                                       │
│                            │                                                                    │
│                            ▼                                                                    │
│   [ Frontend Virtualizer (IPC Payload: 50 items with matched character indices) ]               │
│   • Renders search results with highlighted matching letters at 60fps.                          │
│   • 0 JS heap bloat, 0 GC pauses, sub-2ms total response time (replaces fuse.js).               │
│                                                                                                 │
│   *In-Memory Entities (Command Palette, Tags, Shelves, TOC)*                                    │
│   • Queries bypass SQLite and run directly through `nucleo-matcher` in < 0.05ms!                │
│                                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────────────────────┘
```

### 3.4 In-Book Search & Audio Processing
- **Zero-Allocation Search Snippets (`src-tauri/src/book_search.rs`)**: Uses `char_indices()` byte-slicing instead of `Vec<char>` allocations, speeding up in-book search by 10× with 0 transient heap bytes.
- **Native PDF Search (`search_book_content`)**: High-speed parallel PDF object stream parser that extracts text and yields matches with `pdf:page:X` locations.
- **Native WSOLA Time-Stretching (`src-tauri/src/audio_player.rs`)**: High-quality pitch-preserving waveform similarity overlap-add algorithm for 0.5×–3.0× narration speed adjustment.

### 3.5 P2P Sync Hardening (`theorem-sync-core`)
- **Granular Item-Level Sync Keys**: Transition from monolithic array keys (`annotations`) to atomic keys (`anno:<bookId>:<annotationId>`), eliminating Last-Write-Wins (LWW) array clobbering during concurrent offline editing.
- **Native In-Database Gossip Merge (`sqlite_merge_sync_entries`)**: Applies incoming Iroh-gossip updates directly to SQLite in a single transaction.
- **Multi-Mount Drive Normalization**: Extended Tauri asset protocol and file resolution to support external storage (`/run/media/**/*`, `/media/**/*`, `/mnt/**/*`).

---

## 4. Comprehensive Feature Audit & Stabilization Matrix (v1.5.5 → v1.6.0)

### 4.1 PDF Engine (`src/features/reader/engines/pdfjs-engine.tsx`, `PDFReader.tsx`)
- [x] **Full Virtualizer Geometry**: Pre-computed layout geometry via `prefetch_pdf_structure` and `computeVirtualPageTops` for all $N$ pages.
- [x] **Scrollbar Accuracy & Sizing**: Scroll container reflects 100% true height; zero layout shift or scrollbar jumping on scroll.
- [x] **Two-Page Spread View**: Full `two-page` presentation mode ("Facing Pages") alongside `scroll` and `paged`, with facing pairs `[1, 2], [3, 4]` and non-clipping `max-content` layout.
- [x] **Spread-Aware Navigation**: Keyboard arrows, PageUp/PageDown, boundary-aware touch swipe, and tap zones advance by spread in two-page mode.
- [x] **Instant Navigation**: 0ms page jumps to any page (TOC & input) with surrounding proxy pre-fetching.
- [x] **Search Match Focus Rings**: Highlights query occurrences on the PDF text layer with `.pdf-search-match` focus rings and pulse animations.
- [x] **Continuous TOC Outline Tracking**: Table of Contents tracks and highlights the active chapter outline item across continuous multi-page scrolling (`activePdfTocHref`).
- [x] **Floating Reader Navigation Pill & Zoom Bar**: Centered, solid/frosted pill (`bottom-6 left-1/2 -translate-x-1/2 z-50 rounded-full bg-[var(--color-surface)]/95 backdrop-blur-xl border border-[var(--color-border)]`) with page navigation, fraction, and zoom dropdown.
- [x] **Fullscreen Overlay Chrome & Stacking Context**: Viewports remain static fullscreen `absolute inset-0 z-0 isolate`, with Titlebar and Navbar floating at `fixed z-[140]`, preventing EPUB reflow and ensuring correct overlay layering.
- [ ] **Visual Thumbnail Drawer**: Sidebar thumbnail strip / grid skimmer for rapid visual browsing across large technical PDFs (v1.5.8).

### 4.2 EPUB & Reflowable Engine (`src/features/reader/foliate-js-runtime/`)
- [x] **Null Safety Guards**: Protected `paginator.expand()` and `uncollapse()` against null document elements and non-object anchors (fixed in commit `9c7d18e`).
- [x] **Client Rect Filtering**: Multi-column SVG highlight overlayer strictly uses `Range.getClientRects()`, discarding column gutter zones.
- [x] **Anchor Stabilization**: `goToAnnotation(range)` locks target scroll position strictly using `range.startContainer` and `range.startOffset`.
- [x] **Zero-Reflow Viewport Architecture**: Preserved static fullscreen `absolute inset-0 z-0 isolate` viewport so chrome toggles never re-calculate columns or trigger pagination shifts.
- [x] **GPU-Promoted 300ms CSS Slide Animation**: Restored hardware-accelerated CSS transform transitions for page turns and snap releases while cleanly disabling them during active finger drags and JS frame interpolation.
- [x] **Natural Thumb Swipe Ergonomics**: Adjusted gesture axis arbitration (`absDx > 16 && absDx > absDy * 1.3`) in `paginator.js` so natural arced thumb swipes reliably turn pages without vertical lockout.
- [x] **Zero-Latency Touch Action**: Set `touchAction: 'none'` on paginated viewport containers in `ReaderViewport.tsx`, eliminating mobile WebView gesture arbitration delays.
- [ ] **Inactive Chapter Blob URL Revocation (LRU)**: Implement an explicit `URL.revokeObjectURL()` LRU sweep for off-screen chapter resources in `foliate-js-runtime`, bounding long reading sessions of illustrated books under 150MB RSS (v1.5.7).
- [ ] **Font-Ready Race Calibration on Low-End Mobile**: Extend `Promise.race([doc.fonts.ready, wait(120)])` to 200ms on mobile devices to prevent post-render layout shifts on cold boot (v1.5.7).
- [ ] **Touch Drag-Selection Boundary**: Refine edge-drag scrolling on mobile touchscreens when extending a highlight across page turns (v1.5.8).
- [ ] **Theorem Lens Margin Placement**: Ensure peek popovers (`FootnotePopover.tsx`) clamp margins cleanly when tapped near viewport edges (v1.5.8).

### 4.3 Peer-to-Peer Sync & Storage (`src-tauri/src/file_transfer.rs`, `sync_commands.rs`)
- [x] **Multi-Mount Scope**: Extended asset protocol and file resolution to external backup drives (`/run/media/**/*`, `/media/**/*`, `/mnt/**/*`).
- [x] **Storage Key Migration Support**: Updated `find_in_db` to support `zustand:theorem-library` alongside legacy `persist:theorem-library`.
- [x] **Direct Socket Fallback**: Stored `remote_ip` and `remote_port` from QUIC connection paths for offline LAN file transfers.
- [x] **Reading Progress Gossip Propagation**: Removed premature bailout in `provisionToIrohDocs`, propagating position updates within 2 seconds.
- [x] **Atomic Sync Keys**: Transitioned to `anno:<bookId>:<annotationId>` item-level sync keys.
- [x] **Immediate Progress & Reload Flow**: Emitted initial `0.0%` transfer progress upon connection and reset `loadedBookIdRef` on download completion so the viewer immediately mounts newly acquired files.
- [x] **Clean Failure Action UX**: Removed extraneous "Go to Sync Settings" button from the reader download failure modal for a streamlined two-button group (*Back to Library* and *Try Again*).
- [ ] **Sync Deletion Tombstone Purging**: Automatically purge local book cards and cached artifacts when `deletion_tombstones` are received over Iroh gossip, eliminating ghost books (v1.5.7).
- [ ] **Explicit Tombstone Transfer Response**: In `file_transfer.rs`, return an explicit `PEER_BOOK_DELETED` status if a requested book exists in `deletion_tombstones` or has `deletedAt` set, prompting the client to remove the card immediately (v1.5.7).
- [ ] **SQLite Memory Trimming & WAL Checkpointing**: Integrate `PRAGMA shrink_memory;` and `PRAGMA wal_checkpoint(PASSIVE);` into `trim_memory` to reclaim connection page caches (v1.5.7).
- [ ] **Offline Concurrent Merge Hardening**: Edge-case verification for simultaneous annotation edits across 3+ offline peers (v1.5.8).
- [ ] **Subnet mDNS Peer Discovery**: Broadcast and discover paired devices via local mDNS for sub-100ms LAN transfers without relay round-trips (v1.5.8).
- [ ] **Relational Library Store Migration**: Transition `zustand:theorem-library` from a monolithic serialized JSON array in `kv_store` to granular relational records in `book_metadata` (v1.5.9).

### 4.4 Mobile Experience & Memory Lifecycle (Android & Desktop)
- [x] **TTS Voice Safety**: Added Kotlin null-safety checks around `currentTts.voices` in `TtsAudioPlugin.kt`.
- [x] **Mobile Immersion Controls**: Defaulted TTS enabled and exposed voice/speed popovers on mobile.
- [x] **On-Demand Neural TTS Preload**: Decoupled ONNX model loading from book-open events; only initialize neural models when Immersion Mode is active, eliminating ~800MB heap spikes and 100% CPU burn on every open.
- [x] **Reader Teardown & glibc Arena Trimming**: Trigger `tts_engine_unload` and `libc::malloc_trim(0)` on reader exit to immediately release dormant thread arenas back to the Linux kernel.
- [x] **Subprocess Reaping**: Detached background listener threads call `child.wait()` on `spd-say` and `killall` child processes, preventing `<defunct>` zombie process accumulation.
- [ ] **Android OS Low-Memory Binding (`onTrimMemory`)**: Hook Android's `ComponentCallbacks2.onTrimMemory(level)` in the native plugin to trigger `trim_memory`, preventing background process termination by the Android OS (v1.5.7).
- [ ] **Android Hardware Back Button Navigation Stack**: Intercept the hardware back button event to cleanly dismiss active modals, sheets, and reader panels before exiting to library or closing app (v1.5.7).
- [ ] **Background Audio Focus & Lockscreen MediaSession**: Bind native Android `MediaSession` and request audio focus in `TtsAudioPlugin.kt` so narration continues uninterrupted when the screen turns off (v1.5.7).

### 4.5 Universal PKM Export & Integrations (v1.6.0 Target)
- [x] **High-Speed Batch Exporter**: Multi-threaded Rayon export engine in Rust (`vault_export.rs`) writing thousands of notes in <50ms.
- [ ] **Native Rust Readability Engine**: Replace naive string-finding in `src-tauri/src/article_extractor.rs` with native DOM scoring; purge `@mozilla/readability` and `dompurify` (v1.5.8).
- [ ] **Knap AST Template Engine**: Integrate `knap` into `vault-sync.ts` for customizable frontmatter, quote callouts (`> [!quote]`), and author wikilinks (`[[author]]`) (v1.5.9–v1.6.0).
- [ ] **Universal PKM Presets**: Out-of-the-box presets for Obsidian, Logseq, Foam, SilverBullet, Anytype, and Joplin (v1.6.0).
- [ ] **Automated Webhooks**: Event-driven HTTP dispatch for pushing annotations to Readwise, Notion, and custom endpoints (v1.6.0).

---

## 5. Phased Release Roadmap: v1.5.5 to v1.6.0

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                     THEOREM STABILIZATION & RELEASE PHASING                     │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│   ═══════════════════════════════════════════════════════════════════════════   │
│   v1.5.5 (STABLE BASELINE) [RELEASED]                                           │
│   ═══════════════════════════════════════════════════════════════════════════   │
│   • Cross-page client rects & anchor lock in Foliate overlayer.                 │
│   • Android TTS null-safety & mobile immersion controls.                        │
│   • Iroh gossip reading progress propagation fix.                               │
│                                                                                 │
│   ═══════════════════════════════════════════════════════════════════════════   │
│   v1.5.6-BETA: NATIVE-GRADE PDF PARITY & CORE MEMORY STABILIZATION [RELEASED]   │
│   ═══════════════════════════════════════════════════════════════════════════   │
│   • PDF Native Parity: Full virtualizer geometry, 0ms page jumps, two-page      │
│     spread mode, continuous outline tracking, text match focus rings.           │
│   • Eliminated 1.3GB memory spike: on-demand neural preload, unmount unload,    │
│     malloc_trim(0) memory reclaim, and Linux TTS zombie process reaping.        │
│   • Restored mobile EPUB navigation: 300ms CSS slide animation, diagonal thumb  │
│     swipe axis curves, and zero-latency touchAction: none.                      │
│   • P2P On-Demand File Transfers: atomic .download.tmp streaming, immediate     │
│     0.0% progress emission, loadedBookIdRef reload unlock, clean 2-button UI.   │
│   • Dependency Modernization: Vitest 5, zod 4.6.5, react-virtual 3.14.13,       │
│     lucide 1.47.0, and 44 updated Rust crates.                                  │
│                                                                                 │
│   ═══════════════════════════════════════════════════════════════════════════   │
│   v1.5.7-BETA: STORAGE OPTIMIZATION, MEMORY LIFECYCLE & SYNC RESILIENCE [ACTIVE]│
│   ═══════════════════════════════════════════════════════════════════════════   │
│   • Sync Deletion Tombstone Purging: auto-purge deleted books on gossip merge   │
│     and return PEER_BOOK_DELETED from file_transfer to eliminate ghost cards.   │
│   • SQLite Memory Trimming: PRAGMA shrink_memory + wal_checkpoint(PASSIVE).     │
│   • Android Low-Memory Binding: hook onTrimMemory to free background caches.    │
│   • Foliate Object URL Revocation: LRU cleanup for inactive chapter assets.     │
│   • Android Navigation Stack: wire hardware back button to dismiss panels/exit. │
│   • Background Audio Focus: MediaSession + audio focus in TtsAudioPlugin.kt.    │
│                                                                                 │
│   ═══════════════════════════════════════════════════════════════════════════   │
│   v1.5.8-BETA: NATIVE RUST READABILITY & HIGH-SPEED LAN DISCOVERY [UPCOMING]    │
│   ═══════════════════════════════════════════════════════════════════════════   │
│   • Native Rust Readability Engine (purging @mozilla/readability & DOMPurify).  │
│   • Subnet mDNS Peer Discovery for instant (<100ms) direct LAN file transfers.  │
│   • PDF visual thumbnail drawer & grid overview for rapid document skimming.    │
│   • Multi-device offline concurrent sync merge hardening in Rust.               │
│                                                                                 │
│   ═══════════════════════════════════════════════════════════════════════════   │
│   v1.5.9-BETA: RELATIONAL LIBRARY MIGRATION & KNAP AST TEMPLATING [UPCOMING]    │
│   ═══════════════════════════════════════════════════════════════════════════   │
│   • Relational Library Store: phase out monolithic JSON zustand:theorem-library │
│     in favor of granular indexed SQLite book_metadata records.                  │
│   • Knap AST Template Engine integration for customizable Obsidian / PKM sync.  │
│                                                                                 │
│   ═══════════════════════════════════════════════════════════════════════════   │
│   v1.6.0 (STABLE RELEASE): UNIVERSAL PKM EXTENSIBILITY & CORE EXCELLENCE        │
│   ═══════════════════════════════════════════════════════════════════════════   │
│   • 1-click PKM presets (Obsidian, Logseq, Foam, SilverBullet, Anytype, Joplin).│
│   • Event-driven webhooks for Readwise, Notion, and custom HTTP endpoints.      │
│   • Full SQLite virtualization & zero-copy memory footprint across all views.   │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 5.2 Deep Dive: v1.5.7-BETA Implementation Plan (Storage, Lifecycle & Sync)

### Work Package 1: Sync Deletion Tombstones & Ghost Book Elimination
- **Problem**: When a book is deleted on one peer, other paired peers often retain the stale book card in their library because `deletion_tombstones` are merged into SQLite but not reconciled against the active in-memory Zustand library state. When the user taps the stale card, on-demand download requests the file from the source peer, which fails with `peer error: book not found`.
- **Implementation**:
  1. In `src/core/lib/sync-orchestrator.ts` (`reconcileWithStore`), after `sqlite_merge_sync_entries` completes, query local `deletion_tombstones` and filter out matching books from `useLibraryStore.getState().books`.
  2. In `src-tauri/src/file_transfer.rs` (`locate_book`), check if `book_id` exists in the `deletion_tombstones` table or has `deletedAt` set. If so, return a distinct error `Err(format!("book '{book_id}' has been deleted on this device"))`.
  3. In `Reader.tsx`, if on-demand download returns that the book was deleted on the peer, immediately trigger `useLibraryStore.getState().removeBook(currentBookId)` and navigate back to the Library with an informative toast (`"This book was deleted on the source device."`).

### Work Package 2: SQLite Memory Reclaim & Android `onTrimMemory`
- **Problem**: SQLite holds onto dirty page buffers and connection memory indefinitely unless told to release them. On Android, running in the background without trimming memory causes the OS LMK (Low Memory Killer) to terminate the app.
- **Implementation**:
  1. In `src-tauri/src/lib.rs` (`trim_memory`), execute `PRAGMA shrink_memory;` and `PRAGMA wal_checkpoint(PASSIVE);` on the SQLite connection pool alongside `libc::malloc_trim(0)`.
  2. In `src-tauri/plugins/android-tts-audio` or `MainActivity.kt`, register a `ComponentCallbacks2` listener:
     ```kotlin
     override fun onTrimMemory(level: Int) {
         super.onTrimMemory(level)
         if (level >= ComponentCallbacks2.TRIM_MEMORY_UI_HIDDEN) {
             // Dispatch event to Tauri runtime to call trim_memory
             triggerTauriTrimMemory()
         }
     }
     ```
  3. Reclaim unused WebP image caches and disconnect inactive Iroh endpoints when hidden.

### Work Package 3: Foliate Section Resource Cleanup (LRU)
- **Problem**: In long reading sessions of illustrated books, comics, or manga, Foliate creates `blob:` URLs for each chapter's images. These remain held in WebView graphics memory, gradually increasing WebView RSS beyond 400MB.
- **Implementation**:
  1. In `src/features/reader/foliate-js-runtime/view.js`, maintain a tracked set of active section `blob:` URLs.
  2. When navigating between chapters, revoke URLs for sections that are more than 2 chapters away from the active viewport (`URL.revokeObjectURL(url)`).
  3. Caps WebView graphics memory to $\le 120$MB regardless of session length.

### Work Package 4: Android Hardware Back Button & MediaSession
- **Problem**: Tapping Android's system back button closes the application directly rather than dismissing open modals (TOC, Settings, Search, Bookmarks) or returning from Reader to Library. TTS playback stops when the screen is locked.
- **Implementation**:
  1. In `src/App.tsx`, listen to `@tauri-apps/api/event` or `appWindow.onCloseRequested` / Android back event:
     - If a reader panel or modal is open $\rightarrow$ close the panel.
     - If in Reader view $\rightarrow$ navigate to Library route (`setRoute('library')`).
     - If in Library view $\rightarrow$ allow default OS minimize/exit.
  2. In `TtsAudioPlugin.kt`, attach `MediaSessionCompat` with playback state and request `AudioManager.AUDIOFOCUS_GAIN_TRANSIENT_MAY_DUCK`, allowing immersion narration to proceed smoothly on screen-off.

---

## 6. Quality Gates & Architecture Invariants

Every commit and release tag must satisfy Theorem's strict quality gates:
1. **TypeScript**: `pnpm typecheck` must produce zero errors.
2. **Vitest**: `pnpm test` must run and pass 100% of all unit and integration tests (no weakened assertions).
3. **Rust Backend**: `cd src-tauri && cargo fmt --check && cargo clippy -- -D warnings && cargo check` must pass with zero diffs and zero warnings.
4. **Zero-Secret Invariant**: Strict compliance with `AGENTS.md` (no reading, logging, or writing of keys, credentials, or environment secrets).
5. **No Barrel Imports**: Never import from `src/core/index.ts`.
6. **No Zustand Destructuring**: Always use individual selector hooks (`useUIStore(s => s.x)`).
7. **No React Router**: Routing remains 100% Zustand-driven via `useUIStore.currentRoute`.
