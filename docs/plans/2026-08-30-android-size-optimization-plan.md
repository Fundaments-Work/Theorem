# Android APK Size Optimization (Completed)

**Date**: 2026-08-30 · **Status**: ✅ Implemented (see `feat(android)` commit)

All four pillars shipped:

- **Pillar 1 — Cargo profile**: `opt-level = "z"`, `panic = "abort"`, fat LTO, single codegen unit, symbol stripping. `tokio` `process` feature is desktop-only.
- **Pillar 2 — Frontend**: only `Adobe-*`/`Uni*` PDF.js cmaps ship (74 of 169 tables).
- **Pillar 3 — Gradle**: R8 minify + resource shrink (pre-existing) and `resourceConfigurations` locale filter (en/es/fr). Note: `build.gradle.kts` is gitignored — the locale filter is a local edit and is regenerated by `tauri android init` on CI.
- **Pillar 4 — Packaging**: AAB splits + per-ABI builds already configured.

## Remaining (requires device/CI tooling, not code)

- [ ] Benchmark build on `aarch64-linux-android` and record before/after APK byte sizes in CI (needs Android SDK/NDK runner).
- [ ] Verify `panic = "abort"` on a full release Android build.
